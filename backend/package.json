{
  "name": "backend",
  "version": "1.0.0",
  "description": "Itineos backend Lambda function",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "preload-env": "node scripts/load-env.js",
    "sam:build": "npm run build && sam build",
    "sam:start": "npm run preload-env && sam local start-api --port 3001",
    "sam:start:debug": "npm run preload-env && sam local start-api --port 3001 --debug-port 5858",
    "sam:invoke": "npm run preload-env && sam local invoke RouteFunction",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@google/genai": "^1.34.0",
    "axios": "^1.7.7"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.159",
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
